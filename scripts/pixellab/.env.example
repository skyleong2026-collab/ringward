# Copy this file to ".env" (same folder) and paste your token after the =
# Get a token at https://api.pixellab.ai/mcp
# The real .env is gitignored — it is NEVER committed and NEVER shipped in the web app.
PIXELLAB_API_KEY=
